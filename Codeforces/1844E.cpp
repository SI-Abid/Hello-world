#include<bits/stdc++.h>
using namespace std;

signed main()
{
    int _;
    cin>>_;
    for(int tc=1;tc<=_;tc++)
    {
        // printf("Case %d: ",tc);
    
        int n,m,k;
        cin>>n>>m>>k;
        bool flag = true;
        while(k--)
        {
            int a,b,c,d;
            cin>>a>>b>>c>>d;
            
        }
    }
    return 0;
}